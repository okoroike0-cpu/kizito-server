# ── Node ──────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*
# package-lock.json is intentionally NOT ignored
# It locks dependency versions so Render builds identically to your machine

# ── Environment & Secrets ─────────────────────
.env
.env.local
.env.production

# ── OmniFetch Specific ────────────────────────
# Cookies file may contain authenticated session tokens — NEVER commit this
cookies.txt

# OAuth2 token file — contains your YouTube refresh token — NEVER commit this
*.token

# yt-dlp binary if ever downloaded locally
yt-dlp
yt-dlp.exe

# Temp download artifacts
*.part
*.ytdl

# ── OS Junk ───────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor Junk ───────────────────────────────
.vscode/
.idea/
*.swp
*.swo